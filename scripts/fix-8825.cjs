#!/usr/bin/env node
/**
 * תיקון שגיאות תרגום ב-88.25, שהתגלו בהשוואה מול המקור האנגלי.
 *
 * הסימון שהוביל לכאן: `enrichment-triage` דיווח על "Wei Chieh Young"
 * חסר. המקטע היה שם - אבל תחת שם משובש ("וו צ'ואן יונג"), ובדיקת
 * התוכן חשפה שלוש שגיאות קליניות נוספות.
 *
 * הסקריפט חד-פעמי ונשמר לתיעוד מה בדיוק שונה.
 */
const fs = require('fs')
const p = 'src/data/zones/zone88.ts'
let s = fs.readFileSync(p, 'utf8')

const FIXES = [
  // שם המטפל היה משובש לגמרי, והמשפט על הרגעה והרדמה הושמט
  [
    "וו צ\\'ואן יונג: נקודה חשובה מאוד לטיפול ברוח.",
    "יאנג ויי צ\\'ייה (Wei Chieh Young): נקודה חשובה מאוד להרגעה ולהרדמה, ומפזרת רוח.",
  ],
  // המקור אומר "מקור מטייוואן (לא צוין)" - לא מאסטר טאנג
  ['מאהר: מקור ממאסטר טאנג אומר', 'מאהר: מקור מטייוואן (לא צוין) אומר'],
  // "the point can treat pain" תורגם ל"כף היד" - שגיאה קלינית
  ['ולכן כף היד יכולה לטפל בכאב', 'ולכן הנקודה יכולה לטפל בכאב'],
  // המקור: insomnia and palpitations
  ['לטיפול בנדודי שינה ונוירסתניה', 'לטיפול בנדודי שינה ובדפיקות לב'],
  // ankylosing spondylitis - דלקת, לא ניוון
  ['ספונדילוזיס אנקילוזנטי', 'ספונדיליטיס אנקילוזנטי'],
  // osteophytes in the spine
  ['דורבנות בעצם', 'אוסטאופיטים בעמוד השדרה'],
  // המקור מונה גם סחרחורת
  ['כגון שיתוק פנים, שבץ והמיפלגיה', 'כגון שיתוק פנים, סחרחורת והמיפלגיה (משבץ רוח)'],
]

let n = 0
for (const [from, to] of FIXES) {
  if (!s.includes(from)) { console.error('✗ לא נמצא: ' + from.slice(0, 40)); continue }
  s = s.replace(from, to)
  n++
}
fs.writeFileSync(p, s)
console.log(n + '/' + FIXES.length + ' תיקונים הוחלו')
